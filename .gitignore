__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 临时脚本
analyze_pdf_structure.py
extract_key_pages.py
extract_pdf_text.py
pdf_analysis.txt
pdf_key_pages.txt

# 本地数据（保险公司格式注册表，运行时生成）
insurance_agent/data/format_registry.json

# 提取结果（运行产物）
extraction_results.json

# 敏感/外部资料
inputs/
test_pdfs/

# 环境变量注入（含 MQ 密钥，禁止提交）
.env
mq_bridge_dlq.jsonl

# 飞书 webhook 配置（Bearer Token 敏感，禁止提交）
.feishu_webhook.json
feishu_webhook_dlq.jsonl
feishu_webhook_pusher.log

# 通知发送审计日志（运行产物，可选：用户希望跟踪历史，暂不排除）
# notification_audit.jsonl

# Cloudflare Tunnel 二进制（本机运行用，勿提交）
cloudflared.exe
cloudflared-config.yml

# Cloudflare Tunnel 运行时日志（每次启动生成）
tunnel.log
tunnel_url.txt
tunnel_stdout.log
tunnel_stderr.log
